<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MVC Architecture - View Layer</title>
</head>
<body>

    <h1>Blog List</h1>
    <ul>
        <% blogs.forEach((blog) => {%>
            <li>
                <a href="/blog/<%= blog.id %>"><h2><%= blog.title %></h2></a>
                <!-- <a href = "/blog/<%= blog.id %>/<%= blog.title.replace(/\s+/g, '-') %>"><h2><%= blog.title %></h2></a> -->
                 <!-- The above commented line is is for SEO purpose , to view blog title in URL itself -->
                <p><%= blog.content %></p>
            </li>
        <% }) %>
    </ul>
    
</body>
</html>